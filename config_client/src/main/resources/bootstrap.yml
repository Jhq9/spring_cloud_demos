spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8093/
      profile: default
      label: master
server:
  port: 8094
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8084/eureka/